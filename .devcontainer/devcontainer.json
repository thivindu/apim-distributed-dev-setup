{
  "name": "Java 11 with MySQL",
  "dockerComposeFile": "docker-compose.yaml",
  "service": "apim",
  "workspaceFolder": "/workspace",
  "forwardPorts": [9443],
  "customizations": {
    "vscode": {
      "extensions": [
        "redhat.java",
        "vscjava.vscode-java-pack"
      ]
    }
  },
  // "postCreateCommand": "sudo apt update && sudo apt install -y maven",
  "remoteUser": "root"
}
